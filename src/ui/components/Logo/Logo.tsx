import React from 'react'
import styled from 'styled-components'

const LogoDiv = styled.div`
    font-size: 25px;
    color: #fff;
`

const Logo = () => (
    <LogoDiv>
        ForCallstack
    </LogoDiv>
)

export default Logo